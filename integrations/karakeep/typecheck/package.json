{
  "name": "facetmark-karakeep-typecheck",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Typechecks ../search-facetmark against hand-written stubs of karakeep's plugin interfaces. Not shipped, not imported by karakeep.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "check-drift": "bash ./check-upstream-drift.sh"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}

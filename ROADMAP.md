# 项目状态

一句话：**W1 做完了，W4 做完了（结论是否定的），W2/W3 六个开关判过胜负，唯一改了默认值
的那个又被一批对抗性查询否掉、默认值已退回无门控行为，融合本身仍然没修好。** 现在这份代码是可安装、
可跑、有测试、有 CI、有完整评测报告的软件；它的默认配置里每一个 flag 都挂着一个 p 值，
但它仍然不是一个在别人的书签库上证明过检索质量的软件。

## 已完成

| | 状态 | 依据 |
|---|---|---|
| 数据层 | 完成 | SQLite + sqlite-vec，schema v4，四次迁移都有测试 |
| 采集 | 完成 | 书签导入、遵守 robots 的抓取、两级正文抽取（trafilatura → readability） |
| 富化 | 完成 | 摘要 / 主题 / 实体 / 意图查询，2,375 / 2,376 篇成功 |
| 索引 | 完成 | 内容与意图向量、会话切分、四类图边 26,697 条（评测所用图，见下方「已知会让人踩到的地方」） |
| 检索 | 完成 | 五个消融档位 + 十三个探索/诊断档位，融合、上下文乘子、图扩展、LLM 重排 |
| 接口 | 完成 | CLI、HTTP API、MCP 服务器（9 个工具）、karakeep 搜索插件 |
| 测试 | 完成 | 1,081 项测试，全部离线可跑 |
| CI | 完成 | 2 系统 × 2 Python 版本、ruff、覆盖率、扩展构建、MCP 真管道 |
| **W1 决策闸门** | **完成** | `docs/gate-w1.md`：2,376 篇真实网页 / 479 条查询 / 真实 3B 模型与 bge-m3 |
| **W4 意图面判读** | **完成** | `docs/w4-intent-read.md`：协议先行、50 条逐条判读，会 38%，判为理念问题 |
| **W2/W3 开关判定** | **完成** | `docs/gate-w2w3.md`：616 条全新查询、十档、Holm 校正；`A_gatedctx` 过上线检验（+3.09pp，19 胜 0 负），**默认值据此改了一次** |

W1 的结论是**三条预注册判据全部未通过**，并且失败方向比通过更有信息量：不是某个机制
不行，而是平权 RRF 融合本身在往一个已经很强的检索器里灌噪声。默认配置据此从
「四面融合 + 上下文 + 图 + 重排」改成「内容面 + 图扩展 + 时间衰减」，每个 flag 在
`search/pipeline.py` 的注释里都挂着让它这么设的那个 p 值。

### W4：意图面到底成不成立——**做完了，答案是不成立**

全文与逐条记录：`docs/w4-intent-read.md`、`eval/w4-intent-read.jsonl`。协议（抽样
种子、三档标准、判定阈值）在看数据之前就写死并落盘了。

抽 50 条 `kept=1` 的意图查询逐条判读：**会 38% / 勉强 42% / 不会 20%**。预注册规则是
「会 < 50% 判为理念问题」，所以判为**理念问题，不是模型不够大**。

关键的不是那个百分比，是失败的形态：彻底错的只有 20%（这一档换大模型能修），占最多
的 42% 是**在已知答案的前提下反推**——逐字复制标题、产品名加泛后缀、抄页面里的专有
名词。更大的模型只会写出措辞更漂亮的标题改写，因为它能看到的只有这一页，写不出
「这个人当时为什么存它」。

顺手做的一个零成本检验（`scripts/facet_overlap.py`，全库 2,376 个探针）：用同一个
内容向量同时探两个面，去掉探针自身，意图面与内容面的 top-10 重叠 **26.9%**，而一个
真正不同的面（词面）只有 10.6%，随机是 0.4%；2,367 / 2,376 个探针（99.6%）离自己
内容向量最近的意图向量就是它自己生成的那几条。意图面不是内容面的复制品，但它与内容
面的相关性是别的面的 2.5 倍，而 W1 里加上它是 −5.43pp。

**没有据此改任何默认值**（默认档本来就只开内容面），也没有删意图抽取。

一个诚实的限制：判读者是这套系统的 LLM 助手，不是这个书签库的主人，所以 38% 是**上界**。

### W4 续：那条「标题修复」假设——**也验完了，同样不成立**

全文：`docs/w4-intent-strata.md`，数据 `eval/intent-strata.json`，脚本
`scripts/intent_strata.py`。判读留下的假设是「意图抽取的价值集中在标题退化的页面上，
应当当标题修复用」。原以为要等 W2 的新查询集，实际上用已有数据就能判，**协议这次是
单独一次提交推送的**，先后顺序可以从 `git log` 外部核对。

- 标题退化层（≤3 词，n=213）的 C_nolex − A 是 **−3.76pp**，CI95 [−8.45, +0.94]；
  交互项 +3.01pp，CI95 [−3.38, +9.40]。两个 CI 都含 0，**判为不支持**。
- 唯一像证据的地方（正常层显著负、退化层不显著）来自 `q_episodic` 上 3 条查询的差；
  **意图面本该擅长的 `q_vague` 在两层上一模一样**（−7.81 对 −8.33）。
- 把「退化」收紧到单词标题（n=89）时负得最狠（−7.87pp）。H 要求单调变好，实际相反。
- 全库层面：正文贫瘠页面上意图面与内容面的邻域重合度 28.3%，跟全库的 26.9% 没差别
  ——它没说出内容面缺的东西；而那些页面上**62.4% 的意图用词在页面上根本不存在**
  （正常正文只有 21.3%）。

一句话：**页面自述充分时意图面稀释内容面，页面自述贫瘠时它编造。** 这条假设关闭。

副产品比判定更有用：**W1 的 479 条查询只落在 219 个不同的 gold 页面上，占全库 9.2%。**
抽样框是 `char_count >= 800` 的 1,562 页（65.7%，来自 `gen_queries.py --min-chars` 的默认
值 800，gold 正文最小 802 字符正是这个门槛的印子），被标了正确答案的只有其中 219 页。
干扰项池仍是完整的 2,376 页，这没问题；有问题的是**三条闸门判据是在这 219 页上判的**。
盲区的主体不是那个参数，而是 `char_count = 0` 的 **500 页（21.0%）**——抓取失败、登录墙、
纯前端应用、PDF。把 `--min-chars` 放宽到 300 只多 165 页，碰不到它们。

### W1 续：这批查询里有多少条根本不需要向量——**量出来了**

全文：`docs/query-set-lexical-audit.md`，数据 `eval/lex-solvable.json`，脚本
`scripts/lex_solvable.py`。生成器里最长的注释都在讲防泄漏闸门，但**残余从来没有量过**。
诊断档位 `lex_only`（只有 `lex_seg` + `lex_tri`，一次向量都不算，所以在没有模型权重的
机器上也能跑）在同一批 479 条查询上的结果：

| 查询类型 | n | `lex_only` R@5 | A（仅内容向量）R@5 | 预注册线 |
|---|---|---|---|---|
| `q_content` | 171 | **0.8012** | 0.9591 | ≥ 0.60 → 跨过 |
| `q_vague` | 136 | **0.4632** | 0.7059 | ≥ 0.30 → 跨过 |
| `q_episodic` | 172 | 0.1744 | 0.2791 | 不设线 |

**协议（含三条线）是单独一次提交推送的**（`f46e021`），结果在之后追加。

- `q_content` 里 171 条只有 **2 条**是词面和向量都找不到的——天花板 98.8%，A 是 0.9591，
  而一个不含向量的档位已经 0.8012。这一类几乎不区分任何东西。`check_content` 要求用
  正文里的词，而正文本身是被索引的。
- `q_vague` 有 46.3% 词面可解。闸门禁的是稀有词（df < 1%），管不着常见词的组合
  （`problems with building desktop apps` 这种，词面 rank 1、A 未命中）。C−B 那条判据
  全部建立在这一类上。
- **推论边界**：「词面可解」不等于「泄漏」。vague 查询被 BM25 找到可能只是正文真的用
  日常词讲了这件事。被否掉的是闸门的隐含主张，产出的是限定条件，不重开任何 W1 判定。
- 顺带：`q_episodic` 有 **64.0% 两条路都找不到**。这一类的问题不是"哪个面更好"。

最有用的一格在互补性四格表里，见下面 W2。

### W2 预备：把融合这一步拆开——**部件都摆出来了**

全文：`docs/w2-fusion-anatomy.md`，数据 `eval/lex-facet-split.json`、
`eval/fusion-arith.json`。探索性、未预注册、不判胜负。

**顺手修掉一个真缺陷：`lex_tri` 在中文查询上从来没有工作过。** `build_fts_query` 的
三元组分支原来只按空白切词——中文句子没有空格，整句被当成一个引号短语丢给 trigram
索引，等于问"哪一页逐字包含用户写的整句话"。479 条查询里，`lex_tri` 返回空的 **186 条
全部是中文**；211 条中文查询只有 **25 条（11.85%）**拿得到候选，修好（重叠 3-gram）后
是 **202 条（95.73%）**。**这个面存在的全部理由就是那个 CJK 盲区，而它在 88% 的中文
查询上缺席**，840 项测试和所有评测数字都没发现——因为从来没人看过它**单独**的输出。
修好之后整体 R@5**一动不动（+0.00pp）**：修 bug 是对的，记成质量改进是不诚实的。

**Facet 3 两半第一次分开打分**（新增诊断档 `seg_only` / `tri_only`，都不需要模型）：

| 分组 | n | `seg_only` R@5 | `tri_only` R@5 | 融合 R@5 | 融合 − seg | CI95 |
|---|---|---|---|---|---|---|
| 全部 | 479 | 0.4843 | 0.4572 | 0.4802 | −0.42pp | [−2.09, +1.25] |
| 中文 | 211 | 0.2607 | 0.2464 | 0.2701 | +0.95pp | [−1.90, +3.79] |
| 拉丁 | 268 | 0.6604 | 0.6231 | 0.6455 | −1.49pp | [−3.73, +0.37] |

六个分组的 CI 全部包含 0，正确说法是**与零不可区分**，**不是**"应该删掉 `lex_tri`"：
四格表里有 13 条只有 trigram 能解（融合只保住 8 条）。

**RRF 的算术（不依赖任何数据）**：单面第一名的得分上限是 1/61 = 0.016393；一个被
**所有面都排在第 50 名**的文档，B 档拿 1.50×、C/D 档拿 2.05×。两个满权重面共同持有的
文档，名次好于 **62** 就赢过单面第一——而候选深度只有 50，**所以它必然赢**。要靠
CombMAX 项恢复这条最弱的保证，需要 λ ≥ 1.116（B）/ 2.361（C），即 max 项压过整个
sum 项。**纯加性打分融合给不出单面保护。** 这个算子已经实现（`rrf(max_bonus=)` +
`C_max` 档位，默认关闭，λ 由 `guarantee_bonus()` 算出），实现过程中还测出一条反直觉
性质：所需 λ 随候选深度**下降**，深度 ≥ 166 时归零——**是候选池浅在制造这个失效模式**。
但交叉排名 165.7 不随深度变，机制本身没有被深度解决。

**掩埋是真发生的**（两个词面面重放 479 条，两面是最温和的情形，所以是下界）：102 个
单面独有的第一名，融合后中位名次 15，**79.4% 掉出前 5**；gold 目标里 244 条被某面排进
前 5，其中 **15 条被融合挤出**。

## 未完成

### W2：修融合，并验证三个已经写好的开关

**六个开关已经判过了**（`docs/gate-w2w3.md`，616 条全新查询，写查询的模型换成
`z-ai/glm-5.2`，两个家族各自 Holm 校正）。结果分成三堆：

- **一个上线了**：`A_gatedctx` vs `A` **+3.09pp**，CI95 [+1.79, +4.55]，**19 胜 0 负**，
  且过了预注册的分层前置条件（`q_content` +0.00pp CI [0,0]，`q_episodic` +8.48pp
  CI [+4.91,+12.05]）。W1 的 −9.94pp 没了、+8.14pp 留住了，`FULL` 据此加上
  `context=True, context_gate=True`。**未被检验的是门控的精确率**，见该文 §7(a)。
  → **这一条后来被推翻了**：那个「未被检验」的精确率在 1.3.0 用 361 条对抗性探针量了，
  门控 361/361 全响、代价 −18.83pp，默认值已退回无门控（`docs/gate-precision.md`）。
  这一段保留原文是为了让「当时依据什么做的决定」可查，不是当前状态。
- **三个赢了机制、输了上线**：`C_notri` +4.54pp、`C_max` +4.22pp、`C_lowlex` +4.22pp
  （都是相对 `C`），但相对 `A` 仍是 −3.4 到 −3.7pp。**融合没被救回来**，只是被解释清楚了。
- **两个什么都没发生**：`C_abstain` 616 条里只改了 **1 条**（`abstain_margin=0.25`
  在真实分数分布上基本不触发）；`D_gated` vs `D` 是 7 胜 8 负——门控对 `A` 有用，
  不是普遍有用。

**所以 W2 的正题仍然开着**：三个 CombMAX / 降权 / 删面的改法都不足以让四面融合追上单面
内容检索。下面这段记录的是"为什么先试这些"，它仍然成立；缺的已经不是查询集了，是一个
**融合规则**层面的改动。

（历史记录：开关已经落地——`Config.weight_overrides`、`Config.context_gate`、
`Config.abstain_margin`、`Config.max_bonus`——六个候选档位可以直接 `--config` 调用，
除门控进了 `full` 以外**默认全部关闭**。当初不判胜负的理由是：提出「弱面该降权」的证据
来自 A→C_nolex 的 −5.43pp，提出「上下文该门控」的证据来自 A_ctx 的 +8.14pp / −9.94pp，
两个数字都出自同一批 479 条查询；在这批上跑出胜负只是在测量循环论证。详见报告
§9.5、§9.6。这一条现在已经解掉了。）

**但处置的先后顺序已经被词面审计改了。** 那份审计不比较任何新开关的胜负，只描述已有
档位之间的关系，所以它能在旧查询集上做。互补性四格表（`lex_only` 命中 × A 命中）：

| 全部（n=479） | A 命中 | A 未命中 |
|---|---|---|
| `lex_only` 命中 | 201 | **29** |
| `lex_only` 未命中 | 107 | 142 |

**29 条（6.05%）只有词面面能找到**，越过预注册的 5% 线（选 5% 是因为 A→B 的绝对损失是
5.43pp，同量级的收益空间才值得为它改融合）。这把两种病因分开了：词面面**不是**没东西
可贡献，是 **RRF 把两个各自能解的列表合成了一个更差的列表**。于是：

- `C_lowlex`（把词面面降权到 0.3/0.2）是**减损方案**——压住噪声的同时会把这 29 条一起
  扔掉，代价和收益同量级。它不该是第一个试的。
- 该先试的是**融合规则**本身，或候选级的择一：让一个面独有的强命中不被另一个面的平庸
  排序稀释。RRF 的倒数排名对"只有一个面认识这个文档"这种情形天生不敏感。
- 这笔钱按类型分在 `q_vague`（10 条，7.35%）与 `q_episodic`（14 条，8.14%），不在
  `q_content`（5 条，2.92%）。

**融合解剖（上面那一节）又把"该先试什么"从一句直觉变成了一个带数的设计约束**：任何
补丁都必须处理"两个满权重面的联合召回在候选深度内必然压过单面确信"这条算术事实，而在
纯加性打分里做到这点需要 λ > 1 的 max 项。这指向 CombMAX / 分数归一化融合 / 候选级
择一，**而不是继续调权重**。同一节还实现了两个档位：`C_notri`（C 去掉 `lex_tri`，与
`C_lowlex` 降权两半是不同主张）和 `C_max`（CombMAX 项，λ 由 `guarantee_bonus()` 闭式
算出而非手写）——同样默认关闭、同样不判胜负。

顺序变了，**门槛没变**：任何融合改动的胜负仍然必须在新查询集上判，这里只确定先做哪个。

新查询集有两条硬性要求（来自 `docs/w4-intent-strata.md` §2.6），**生成器这一侧都已经
做完**：

1. `--min-chars` 默认值从 800 放宽到 300，抽样框从 65.7% 提到 72.7%。
2. `--bodyless-share` 从 `char_count = 0` 的 500 页里另抽一池目标，材料只有标题、地址里的
   词、`folder`、保存时间和同会话邻居的标题。这一池**不出 `q_content`**——正文没抓到，
   「记得这页讲了什么」只能编；也**把地址当泄漏词**，否则 vague 查询复用 slug 就被词面
   直接解决了（v1 的 `q_content` 抄标题拿 100% 命中，就是这个坑）。默认按库里自己的
   比例抽，W1 库上是 22.5%。

**这两条在新查询集上都生效了**（`eval/queries/w2w3-holdout.jsonl`，616 条，
`--min-chars 300`，按库比例抽无正文页），查询集在跑任何一档之前就已冻结推送。仍然要
记住的是：无正文页的目标只出 `q_vague` 与 `q_episodic`，所以关于 `q_content` 的任何
结论**依旧不适用于库里那 21% 没抓到正文的页面**。

原本这里还列着第四项「候选门控」——**它已经被算术划掉了**。肇事的错误文档站在两个
弱面的**第 1 名**，把弱面砍到只剩 3 条它都还在，融合结果一位不变；深度上限砍的是第
11 到 50 名，不是出事的地方。能修的只剩降权和弃权，两者都已实现。对照表见报告 §9.6。

### W3：判据本身要重写（介质已量化，改法未选）

报告 §9.2 记了一个方法论问题：同一个上下文乘子，单独测是 +8.14pp（带扩展 +13.95pp，
越过 10pp 预注册线），放进 D−C 里测只剩 +3.49pp。差别在被测介质——D 是四面融合后的
分数，相邻名次间隔大，`MAX_BOOST = 1.60` 推不动。判据是隔着一层会衰减信号的介质在
测这个机制。W2 之前得先把判据写对。

**这句解释本身当时也只是断言，现在量过了**：`docs/w3-criterion-medium.md` /
`eval/boost-medium.json`（词面介质，不需要模型）/ `eval/boost-medium-vectors.json`
（真实向量介质，需要 bge-m3），都由 `scripts/boost_medium.py` 产出，都不需要新查询集。

- **算术**：1.60 在 A 档能跨过整档动态范围的 **79.7%**，在 C/D 档只有 **20.9%**；要在
  C/D 里拿到同等位移力，上限得是 **6.03**。单面档里 1.60 把前 **37** 名直接顶到第一名。
- **实测**（479 条，真实介质 A vs C，**C 与 D 融合面集合相同，所以这就是判据跨越的那个
  介质**）：把第 20 名一步顶到第一名的成功率 A 是 **100%**（1,916 次探查里上限一次都没
  生效，最大只需要 1.311），C 只剩 **2.9%**，**97.1%** 的查询需要大于 1.60 的乘子。
  从第 10 名起 C 就有 **72.2%** 推不动。衰减主要发生在加上两个词面那一步
  （两个向量面时前 5 名还 100% 夺冠）。
- **先前的词面代理（1 面 vs 2 面）是下界，方向对、量级低**：它给的第 20 名夺冠率是
  60.7%、需要 >1.60 的比例 39.3%，真实介质是 2.9% 和 97.1%。
- **反向提醒**：这个库上（C 锚点）**72.6% 的候选根本没被加成**，被加成时中位只有 1.30，
  顶到上限的只有 1.9%。所以介质解释的是「+8.14pp 为什么缩水成 +3.49pp」，
  **不解释** `q_content` 上的 −9.94pp；后者是乘子发到了不该发的查询上，要靠门控
  （`A_gatedctx`）修，两件事分开。

改法有三条（按档缩放上限 / 改成按位移量定义机制强度 / 换个不受介质影响的作用点），
代价各不相同，文档 §5.1 列了但**不选**。选哪条是 W3 的正题，而且**必须在新查询集上判**。
W1 的三条预注册判定一条都没有重开，第二条仍记「未通过」。

**新查询集给了这件事一个直接的旁证**（`docs/gate-w2w3.md` §5）：同一个门控装在 `A` 上
是 +4.55pp（32 胜 4 负），装在 `D` 上是 **−0.16pp（7 胜 8 负）**，而 `D` 与 `D_gated`
的 `q_episodic` R@5 **同为 0.3214**——在融合后的分数里，这个乘子确实推不动东西。介质
问题不再只是算术论证，它在两个真实档位上留下了可见的差别。**上限该怎么改仍然没有选**。

另有一条已知偏差没有重测：本次评测跑的代码有一个图扩展缺陷（排除集用了整个融合候选池
而不是已入页的 hits），会系统性删掉最好的邻居，所以 `D+exp` 的 40.7% 是**下界**。
缺陷已修（`fc0444f`），但没有在修好的代码上重跑。

### W4 新增：门控还剩两笔账，两笔都需要**新的**查询集

1.3.0 退回无门控之后，关于这个门控还有两件事没量，而且都不能用已有的查询集去量。

- **`gate_v3`：把 `time:relative` 也收窄。** `gate_v2` 已经证明"裸年份不算保存时间信号"
  这条改法有效——它让 197 条裸年份探针的代价精确归零，`p_year` 从 −15.57pp 变成
  −0.50pp，同时保住 616 条上的 +1.79pp。剩下的 −10.52pp 全部来自「最近」「那阵子」
  「三个月前」这类相对时间词，它们同样可能是在讲正文主题（「最近的固态电池进展」）。
  同一把刀砍下去看起来大概率管用——**这正是不能直接做的理由**：那 361 条探针已经被用来
  在 v1 和 v2 之间做过选择，再用它们选 v3 就是重演 `docs/gate-precision.md` 要纠正的
  那种循环。v3 需要自己的预注册和自己的探针集。
- **漏响率一次都没量过。** 整个预注册实验只测了门控**误响**的一面。真实库演示里
  「上次存的那个签到脚本」压根没被判成情景查询（`docs/real-library-demo.md`），说明
  另一面同样有洞：「上次」在 `gate_v2` 的 `_SAVE_ACTION` 里，却不在 v1 的
  `_VAGUE_EPISODIC` 里。要量它，需要一批**确实在找某次保存动作**、却不含年份也不含
  现有词表的查询——同样是新查询集，同样得先写规则再生成。

两笔账合起来才谈得上"这个门控值不值得再上线一次"。任何一笔用旧集子交差都不算数。

## 范围决定：不再自己造检索周边

**2026-08-04 起，这个项目不再自己做浏览器扩展、抓取器和 Web UI。**
[karakeep](https://github.com/karakeep-app/karakeep) 已经把这三样做完了，而且做得更好：
MV3 扩展、iOS / Android App、无头 Chrome 抓取加正文抽取与资源归档、多用户账号、自动
打标、完整 Web UI、Docker 与 Helm 部署。**它唯独把排序做成了插件**，接口只有四个方法
（`addDocuments` / `deleteDocuments` / `search` / `clearIndex`）。

所以分工是：**karakeep 做产品，facetmark 做排序和预注册评测。** 实现见
`src/facetmark/bridges/karakeep.py` 与 `integrations/karakeep/search-facetmark/`，装法与
逐字段映射见 `docs/karakeep.md`。

这个决定顺带解掉一个一直没说破的问题：仓库里那个 `extension/` 是 1.0.0 时构建的产物，
本机没有 Node 工具链，**既重建不了也校验不了**。它留在树里，但不再是这个项目声称维护
的东西。

它还买到一件比省工更值钱的东西：`POST /karakeep/search` 接受 `config` 参数，所以**消融
可以在真实 karakeep 库上跑**。到目前为止 W1 之外的每一个数字都来自生成语料——这是第一条
能拿真实使用去校验它们的路径，也是下一轮实验该走的路。

**尚未做的**：TypeScript 那一侧不在本仓库 CI 里构建（本机没有 Node），多用户过滤发生在
排序**之后**因而系统性偏向书签多的用户，意图面不由桥接填充。三条都写在
`docs/karakeep.md` 里，没有藏。

## 已知会让人踩到的地方

- **没配 embedding 模型时默认档会退化。** mock provider 的 embed 是词级特征哈希，
  内容向量在这种库上是噪声，所以 `default_config()` 会回退到全词面融合，
  `SearchResponse.config` 如实报实际跑了哪一档。请求 `"full"` 收到 `"fused"` 是回退，
  不是 bug。
- **中文语料只占 9.5%。** 评测语料 2,376 篇里正文判为中文的 178 篇。标题含两个以上
  汉字的有 57.7%，但正文大多是英文。中文检索质量的结论强度远低于英文。
- **保存时间戳是构造的。** 情景型查询依赖时间窗，而公开语料没有真实的「你什么时候
  存的」，那一列是按周刊期号推的。`docs/eval-w1.md` §4 记了全部五处偏离。
- **LLM 重排在本地 3B 上是 45 秒一条查询**（p50，max 102 秒）。默认关闭，`--config E`
  可以打开，但那不是交互式延迟。
- **库快照曾经不是评测跑过的那张图（已换掉）。** 第一次发布的
  `w1_corpus/library-w1-indexed.db` 是 `post_index` 重标定**之前**的状态：
  `same_domain 2,593 / semantic 80 / session 16,930 / supersession 45` = 19,648 条，
  语义边覆盖率只有 1.4%。评测跑的图是
  `2,708 / 7,014 / 16,930 / 45` = **26,697** 条，覆盖率 79.4%（`post_index.json` 有记录）。
  在那份快照上重建一次图可以**逐位复现**评测图（已实测：`build_edges()` 把 `same_domain`
  补回 2,708，`build_semantic_edges()` 在 0.93 下给出 7,014，覆盖率 0.7942），
  所以数据本身从来没丢，短的只是那份文件。

  现在发布的快照就是重建之后的那一份（26,697 条，`md5 5c3249a2b1f510a6d68af13c0763d8c3`），
  拿到手可以直接用，不必再跑 `facetmark edges`。如果你手上是旧的那一份（19,648 条），
  跑一次 `facetmark edges` 即可补齐。

  两个根因都已在代码里修掉：
  （1）`edges.SEMANTIC_MAX_DISTANCE` 还停在没人量过的 `0.60`，而评测跑的是按预注册
  规则重算的 `0.93`——也就是说装上这个包复现得了报告的文字，复现不了报告的配置。
  默认值已改成 0.93，并加了钉住它的测试。
  （2）`_insert()` 返回的是「提交了多少条」而不是「表里多了多少条」，这种计数永远不
  可能和写入结果打架，也就永远发现不了漂移；现已改成查表计数，并加了「统计值必须
  等于表内行数」的不变量测试。

- **`SEMANTIC_MAX_DISTANCE` 是嵌入模型的属性，不是这个包的属性。** 0.93 是 bge-m3 上
  「无关文档从哪里开始」的位置。换嵌入模型必须按同一条预注册规则（随机文档对距离
  分布的第 1 百分位，两位小数）重新推一遍，而不是沿用这个数。顺带修掉一个静默陷阱：
  这个常量原先被绑成默认参数，在 import 时就固定了，改 `edges.SEMANTIC_MAX_DISTANCE`
  完全没有效果且不报错；现在改成调用时解析。

- **衰减层在默认档里够不着。** RRF 的分数是 `sum_f w_f / (k + rank_f)`，`rrf_k = 60`
  时单个单位权重的面最高只能给到 `1/61 = 0.016393`；而 `decay_rescue_threshold`
  出厂值是 `0.02`。默认档 `FULL` 恰好是单面配置（`frozenset({"content"})`），于是
  `hot_top_score < rescue_threshold` **恒成立**，救援阀每次都开，它守着的降权一次
  也没执行过。四面的 `FUSED` 不受影响（两个面就有 `1.7/61 = 0.0279`）。
  `tests/test_decay_reach.py` 五条测试把现状钉住了。发现过程见
  `docs/karakeep-roundtrip.md` §4。

  **（a）量过两次，第二次把第一次推翻了。这个意外正在做对的事，而且它承重。**

  第一次（1.5.0，`docs/decay-reach.md`，`eval/decay-reach.json`）：ΔRecall@5 是精确的
  `0.0000pp`，CI95 `[0, 0]`，冷层只有 8 页、0 个目标是冷页，于是记成「代价是零」。

  第二次（1.6.0，`docs/decay-instrumented.md`，`eval/decay-reach-checked.json` +
  `eval/cold-census.json`）：**第一次的库 `health` 表有 0 行**——健康检查器从来没在它上面
  跑过，冷层条件 3 里靠死判定的那一半从来没有可能触发；`open_count` 全库为 0，条件 1
  也是恒真的。那个「三条件检测器」实际只有条件 2 + supersession 边在工作，8 页是这么
  来的。跑完健康检查重做同一个 A/B：冷层 **8 → 73**（`gone 37 + drifted 28 +
  supersession 8`），冷层 ∩ 230 个目标 **0 → 8**（涉及 19 条查询），
  **ΔRecall@5 = `−1.4610pp`，CI95 `[−2.5974, −0.4870]`**，区间不含零。

  机制逐条数得出来：37 条目标名次变化里，**12 条跌出 20 名列表（其中 10 条原本在前 5、
  5 条原本第 1）**，24 条上升（21 条只升一名，只有 1 条因此进前 5），净 `−9/616 =
  −1.4610pp`。收益全落在桶内位移，损失全落在桶边界，这个不对称是结构性的。

  根因：**条件 3 把「URL 死了」当成「存下来的副本没用了」**。facetmark 存正文，404 页的
  那段文字仍然是正确答案；`drifted` 更反——远端改了，本地这份是唯一还留着原内容的地方。

  **所以仍然不改阈值**，但理由第三次改写：从「改默认排序需要协议」→「量过了收益是零」
  → **「量过了，修它要付 1.46pp；这是一个 bug（阈值高于单面档最高分）恰好抵消了另一个
  bug（把死链当无用内容），两个都还在」**。这不构成衰减层设计正确的证据，要真修得改
  条件 3 的语义。

  **（b）阈值该是绝对分数还是按面数归一化——仍然没测，而且现在它有代价了。** 归一化会让
  阀门在单面档里正确工作，而由（a）可知「正确工作」在这个库上等于 −1.46pp。这不再只是
  正确性问题，它是一个会让排序变差的正确性修复，改之前必须先改条件 3。

  **（c）窄化条件 3——新出现的边界，需要新查询集，不能在这 616 条上决定。** 两个候选：
  去掉 `drifted`（冷层 73→45，受损目标 8→5）、或只在没有可服务正文时降权（73→43，
  8→4）。两个都能把 Δ 推回零，**但它们都是看了这批数据的失败案例之后想出来的**，在同
  一批上评估等于在训练集上报测试分。而且第二个改法本身就不干净：8 个受损目标里 4 个
  `char_count = 0`，完全没有正文却仍被检索到并且是正确答案（靠标题和词面面）。

  仪器现在自己会喊：`cold_census()` 把三个条件分开报，`fm stats` 和 `fm health --check`
  会打出 `never_opened_selects_everything` / `health_never_checked` 两条退化警告。这就是
  这件事本来应该在 1.5.0 之前被看见的方式。

## 最有价值的贡献

构造一批新查询集。不用碰检索代码，而它是上面三件事共同的前置条件。
`eval/queries/w1-real-library.jsonl` 是格式参考，`docs/eval-w1.md` 记了这批是怎么造的
以及哪五处造得不够好。

1.6.0 之后这件事又多了一个下游：**窄化冷层条件 3（上一节的 c）也卡在这里。** 而且它对
查询集有一个具体要求——现有 616 条是从**有正文的页面**生成的，所以「找那个已经打不开的
旧页面」这类意图一条都没有。缺了这类查询，冷层与目标集的交集会被人为压小（1.5.0 那次
就是 0），任何关于「降权死链会不会伤到答案」的测量都是虚的。新查询集必须故意包含
死链目标和只有标题可用的目标。

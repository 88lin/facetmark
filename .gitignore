# python
__pycache__/
*.py[cod]
.venv/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# node
node_modules/
extension/dist/
extension/*.zip

# local data - never commit a bookmark library or an index
data/
*.db
*.db-wal
*.db-shm
*.html
!extension/src/*.html
# The pattern above did not stop `favorites_2026_8_4.html`: it arrived through
# GitHub's web uploader, which commits directly and never consults .gitignore.
# Named here as well, so that `git add` at least cannot bring it back.
favorites_*.html
bookmarks*.html
pairing-token.txt

# editors / os
.vscode/
.idea/
.DS_Store

# landing page (GitHub Pages)
!docs/landing/*.html
!docs/landing/*.css
